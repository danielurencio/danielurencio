var sylvester = require("sylvester");

function Net(inputs,layers) {
  this.inputs = inputs;
  this.layers = layers;  
};

Net.prototype = {
  weights: function(this.inputs,this.layers) {
	var inputs = this.inputs;
	
  }
};
